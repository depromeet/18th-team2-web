# 문서 산출물

`/qa` 스킬 실행 후 발견된 이슈 리포트를 이 폴더에 저장합니다.

## 파일명 규칙

| 종류 | 파일명 형식 | 예시 |
| --- | --- | --- |
| QA 리포트 | `qa-YYYY-MM-DD-{기능명}.md` | `qa-2026-04-25-login.md` |

## 저장 방법

`/qa` 스킬 실행 후 이슈가 발견되면 이 폴더에 파일로 저장합니다.
이슈가 없으면 파일을 생성하지 않습니다.
